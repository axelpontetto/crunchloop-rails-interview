<%# Inline edit: swaps the item row (matching turbo frame) into an edit form. %>
<%= turbo_frame_tag dom_id(@todo_item) do %>
  <div class="rounded-lg border border-indigo-300 bg-indigo-50 p-3">
    <%= render "form", todo_list: @todo_list, todo_item: @todo_item %>
  </div>
<% end %>
