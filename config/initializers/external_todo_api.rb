# Base URL and timeouts for the external Todo API are read from the environment
# (see ExternalTodoApi::Configuration). Override here if you need to hard-code
# values, e.g.:
#
#   ExternalTodoApi.configure do |config|
#     config.base_url = "http://todo-api:3001"
#   end
#
# By default it reads EXTERNAL_TODO_API_URL (fallback http://localhost:3001).
